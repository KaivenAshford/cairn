# 复制成 .env 后填写。.env 已被 gitignore。
#
# 多数情况下**不用手抄这份文件**：`scripts/deploy` 会现场生成一份 .env，
# 密钥用 openssl rand -hex 32 当场生成（不回显、不进 shell 历史），
# uid/gid 用 `id -u`/`id -g`，域名问一句。这份文件是给「想自己填」和
# 「想知道每个键是干什么的」两种情况留的。
#
# 起容器时必须带 --env-file：compose 的变量替换读的是**项目目录**下的 .env，
# 而项目目录默认是 compose 文件所在的 deploy/，不是仓库根。
#   docker compose --env-file .env -f deploy/docker-compose.yml up -d

# Caddy 给哪些域名签证书。**缺了容器直接拒绝启动**（compose 里写的是 ${CAIRN_DOMAIN:?}）
# ——与其猜一个域名去签一张谁也用不上的证书，不如停下来问。
# 多个域名用**空格**分隔；逗号不行，Caddy 会直说 Site addresses cannot contain a comma。
# 演练部署填 localhost：Caddy 对 localhost 用内置 CA 签，完全不碰 ACME，
# 也就不消耗 Let's Encrypt 的签发配额（同一组域名每周 5 张，用完锁一周）。
CAIRN_DOMAIN=example.com

# 写入通道的令牌。用 `openssl rand -hex 32` 生成，少于 32 字符服务端会拒绝启动。
CAIRN_TOKEN=

# 服务端监听地址。默认只绑回环，由 Caddy 反代。
# 注意：用 docker compose 时这一行会被 compose 的 environment 段覆盖成 0.0.0.0:8787。
# 容器里绑回环的话，Caddy（另一个容器）一条也连不进来，而症状极具迷惑性——
# 容器 running、healthcheck 还是绿的（探活也在容器内），只有 /api/* 全线 502。
CAIRN_ADDR=127.0.0.1:8787

# 条目落盘位置。这一行是**从仓库根目录跑服务**时的相对路径；
# 而 README 教的是 `cd server && go run .`，那种跑法用服务端内置的默认值即可
# （../web/content/entries），不要 source 这份 .env——否则条目会落进
# server/web/content/entries，一个 HTTP 201、磁盘上真有文件、站上永远不出现的地方。
# 用 docker compose 时这一行会被 compose 的 environment 段覆盖成容器内路径。
#
# 这个目录是单独的 private 仓库，四层可见度的条目都在里面。见 web/content/README.md。
CAIRN_CONTENT_DIR=./web/content/entries

# 容器以哪个 uid/gid 跑。必须是宿主机上那个内容目录的属主，否则写入一律 500
# ——日志里是 permission denied，响应里只有一句 internal error。
# 取值：`id -u` 和 `id -g`。这两个也是缺了就拒绝启动，不给默认值：
# 1000 是个「看着很对」的默认值，恰好也是猜错时最难查的那种错。
CAIRN_UID=1000
CAIRN_GID=1000

# 时区。distroless 镜像里没有 /etc/localtime，不设这个 Go 的 time.Local 就是 UTC，
# 而条目的 created 和 id 里的日期前缀都按它算——晚上写的东西会标成前一天。
# 要的是**写东西的人**在哪个时区，不是机器在哪个时区（VPS 出厂几乎都是 UTC）。
TZ=Asia/Shanghai

# 供 scripts/n 使用，服务端也会读它——Telegram 那句确认里的链接就是拿它拼的
# （没配就只回路径，不猜域名）。
# 域名一共出现在**三处**，换域名时三处都要改：这里、上面的 CAIRN_DOMAIN、
# 以及 web/astro.config.mjs 的 site（canonical 用的是它）。
# deploy/Caddyfile 里没有域名——它读 {$CAIRN_DOMAIN}，就是为了少一处要 sed 的文本。
CAIRN_URL=https://example.com

# ── Telegram 写入通道（可选）─────────────────────────────────────────────────
# 手机上发一条消息 → 站上一条记录。两个都留空 = 不开这条通道，路由根本不注册；
# 只填一个 = **拒绝启动**。半配才是唯一危险的状态：填了名单忘了 secret，
# 等于把一个能往磁盘写文件的端点挂在公网上，靠「没人知道这个路径」活着。
# 开通步骤见 deploy/README.md 的「Telegram 写入通道」——`scripts/deploy` 会替你走完。

# setWebhook 时登记的 secret_token，之后 Telegram 每次推送都会带在
# X-Telegram-Bot-Api-Secret-Token 头里。它证明「这个请求来自 Telegram」。
# 用 `openssl rand -hex 32` 生成；少于 32 字符、或含 A-Z a-z 0-9 _ - 之外的字符，
# 服务端会拒绝启动（后者是因为 Telegram 那边也不收，不然要到调 setWebhook 才发现）。
CAIRN_TELEGRAM_SECRET=

# 允许写入的 Telegram 数字用户 id，逗号或空格分隔。它证明「这条消息来自我」。
# 只认数字，不认 @用户名：用户名能被改掉、放弃后还能被别人注册走。
# 怎么知道自己的 id：`scripts/deploy` 会在登记 webhook 之前先 getUpdates 认一次
# （要你先给 bot 发一条消息）。手动的办法是给 @userinfobot 发条消息。
CAIRN_TELEGRAM_ALLOW=
