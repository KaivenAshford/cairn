# 架构决策

记录**为什么这么选**，而不只是选了什么。改设计前先读这里。

## 1. 静态还是动态：不是二选一

好看程度与这个选择**完全无关**——静态站跑在浏览器里的仍是完整 JS 应用，动画、WebGL、
可交互图表一样都不少。静态唯一做不到的是「每个访客看到的内容不一样」。

判断标准只有一条：

> **这个页面的内容对每个访客是不是一样的？**
> 一样 → 构建时生成；不一样（个性化 / 写入 / 实时）→ 走服务端。

所以默认走静态，按路由开动态。动态站的代价是真实的：会挂且没人值班、运维债的心理成本
（有个要维护的服务在跑，你会潜意识不想碰它，站就死了）、攻击面、常驻成本、
以及数据库里的文章比 git 里的 markdown 难迁移得多。

## 2. 可见度：四层，两条路径

```yaml
visibility: public    # 进 sitemap，走静态
visibility: unlisted  # 长随机路径 + noindex，无需登录，走静态
visibility: circle    # 名单内可见，要登录，走服务端
visibility: private   # 仅自己，走服务端
```

**关键：两条路径、两批产物，不是「同一批静态页 + 登录决定显不显示」。**

`circle` / `private` 的内容**根本不进静态构建产物**。若把私有内容也生成成文件放进同一
目录，它就已经在磁盘上、在 CDN 上了，URL 被猜到或扫到即读走——前端那句
`if (!loggedIn) return null` 拦不住任何人。字节必须由服务端决定发不发。

**先分清两类需求**，能省掉大部分复杂度：

| 类型 | 真实诉求 | 需要 |
|---|---|---|
| 不想被搜到 / 不想被同事随手翻到 | 社交尴尬，不是安全 | `unlisted`，零成本 |
| 泄露了有实际后果 | 真要拦住 | 认证 + 服务端拦截 |

多数「更个人的内容」属于第一类。

**内容和代码分两个仓库。** 代码仓（这个）里没有任何真实条目，只有
`web/content/fixtures/` 那几条示例；真实条目在一个单独的 private 仓库里，
clone 到 `web/content/entries/`（被根 `.gitignore` 忽略）。四层可见度的条目都在那里面。

这么分的理由，按重要性：

1. **消灭唯一一类不可逆的泄露。** 私密条目一旦被提交进公开仓库，git 历史改不掉；
   而「私密内容被构建进了 dist」重新构建一次就好了。分仓之后前一条路径根本不存在。
2. **可见度回到纯粹由 frontmatter 决定。** 曾经它一半由字段、一半由「在哪个仓」决定：
   `unlisted` 跟代码躺在同一个仓库里，它「猜不到文件名」的防护对任何能打开仓库的人
   都不生效——而这件事在目录结构里完全看不出来。
3. 内容仍然有版本历史、仍然能本地用编辑器写、服务器炸了仍然不丢。这几条是当初坚持
   「markdown in git」的理由，换成私有内容仓之后一条都没丢。

**代价是「哪些内容会被构建出去」变成了一条软防线。** 从前是目录分离（硬），现在是
构建时按 `visibility` 字段过滤（软）。这个取舍是有意的：软防线出错的后果落在 dist 上，
可以重新构建；硬防线要防的那条不可逆路径已经被分仓消灭了。代价换来的是可见度模型
只剩一个维度。既然是软的，它就必须有测试钉住——`scripts/test-visibility` 的第 1、2 组
断言 `private` / `circle` 的标题、正文、标签、id 都不出现在 `dist` 的**任何**文件里。

## 3. 认证：不做用户系统

场景是「**我知道我想给谁看**」——一个具体名单，不是开放注册。所以用
**allowlist（邮箱一行一个）+ magic link**：无密码、无用户表、撤销就是删一行。

第一版可以连邮件服务都省掉：一次性登录链接由本人私聊发给对方。

不选 OAuth（把熟人圈绑到第三方，还要处理回调）、不选单一密码（无法区分是谁、
无法单独撤销）。

## 4. 内容模型：只有一种「条目」

不为每种内容建一个系统——那会变成四套系统四份维护，半年后死三套。用**正交属性**：

```yaml
type: post | note | log | link | list | photo   # 长文/常青笔记/短记/链接/清单/影像
visibility: public | unlisted | circle | private
status: seed | growing | done                   # 允许「没写完」也公开
created / updated / tags
```

加一种新内容类型 = 多一个 `type` 值，**架构一行不用改**。展示层按 type 分流：
混合时间流（首页，可筛）、索引页（笔记，不按时间）、卡片网格（书影音）、单页（`/now`）。

`status: seed` 是有意的：它让你能公开还没想清楚的东西。不然每条都得写成文章才敢发。

**条目可以改，而且改法和新建是两种语义。** 写入通道靠请求里有没有 `id` 区分：

| 请求 | 意图 | 行为 |
|---|---|---|
| 没有 `id` | 新建 | id 由标题或随机数生成；撞上已有条目一律拒绝（409），绝不覆盖 |
| 有 `id` | 改那一条 | 没提供的字段沿用旧值，`created` 保留，`updated` 记为今天 |

分开的理由是**覆盖在两种意图下的含义相反**：自动生成 id 的那条路径上，撞名只可能是事故，
覆盖就是静默丢内容；而 `/now` 这种「会被覆盖、不留历史」的单页，覆盖正是它要的。
用同一个语义总有一边是错的。

更新时忘写 `visibility` 会**保持原样**，不是掉回 `private`。「默认 private」防的是新建时
漏字段导致泄露；而把一条已经公开的条目改成私有，是让它从站上悄悄消失——那不是安全，
是数据丢失。两个默认值服务于同一条原则：不因为漏写字段而改变可见度。

## 5. 后端形态

```
Caddy 容器（自动 HTTPS）
  ├── /          → web/dist            静态产物（public + unlisted）
  ├── /api/*     → server（Go）
  │                  └── /api/write（token）← 低摩擦写入通道
  └── /circle/*  → server（Go）        ← 还没实现，一律 501
                     ├── session 中间件（HttpOnly + Secure + SameSite=Lax）
                     └── 从内容目录现渲染（见第 2 节：内容是单独的 private 仓库）
```

`/api/*` 和 `/circle/*` 是两条独立的分流，不是一条。写入通道**现在就在跑**，
而 `/circle/*` 还只是个 501 —— 把它们画在一起会让人以为写入通道要等认证做完才能用。

**Caddy 从宿主机搬进了 compose。** 这一条改过一次，两次的理由都记在这里——
只改结论不改理由的话，下一个人会照着旧理由把它再搬回去。

从前的判断是：「静态产物是文件，让 Caddy 直接从磁盘发就行，多套一个容器换不来任何
好处。」**这句话在当时是对的**，而且现在仍然对——它算的是**运行时**的账，而运行时的
账确实是零：多一个容器、多几处 volume 映射、调试时多一层间接，一样都没少，
发文件也没变快。

变的是目标。当时的目标是「把它跑起来」，那时候「apt install caddy 然后写个 systemd
unit」是一次性成本，摊在一台机器上等于没有；现在的目标是**一键部署**，而一键部署要
算的是**安装那一步**的账：Caddy 在宿主机上就意味着 apt 源、systemd unit、`/etc/caddy`
的属主、各发行版各不相同的包名和路径——这一串不是一次性的，是**每台新机器都要重来
一遍**，而且是一个脚本最难写对、最容易在别人的发行版上崩掉的部分。搬进 compose 之后，
新机器上要人做的事只剩「装 Docker」，其余全在 `docker compose up` 里。

所以不是「原来的理由错了」，是**换了一个要优化的量**：从「运行时的复杂度」换成
「一台新机器到能上线之间的步数」。判断标准变了，结论跟着变；旧标准下的算式一个字
没错，只是不再是要算的那道题。

部署步骤见 [deploy/README.md](deploy/README.md)，代价那一段写在
`deploy/docker-compose.yml` 的注释里（和它要付代价的那几行放在一起）。

- **存储**：先 SQLite 单文件（session 与一次性 token）。不上 Postgres。
- **内容**：始终是 markdown in git，但和代码分两个仓库（见第 2 节）。内容因此也有版本历史、
  可本地写、服务器炸了不丢，而代码仓可以给人看。

## 6. 版式：卡片按 type 分叉

时间流上的一条长文和一条随手记，内容量差十倍。用一条细线把它们分开时，
线上下的东西差得太远，视线找不到「一条」的边界在哪——所以给每条一个实体的卡片，
边界由块本身给出，密度再不齐也读得下去。

**六种 type 长得不一样，这是设计的主张而不是装饰：**

| type | 卡片形态 |
|---|---|
| `post` | 最大的卡：粗标题 + 摘要 + 「读全文 →」 |
| `log` | 最扁的一句话卡，没有标题——正文本来就只有一句，给它挤一个标题只会像没写完的文章 |
| `link` | 带 ↗ 和目标域名。「这条链接通向哪」是外链最重要的一条信息 |
| `list` | 直接铺前几项。清单的信息全在项里，只给标题的话，一张「在读」和一条短记在屏幕上一样重 |
| `note` / `photo` | 走默认卡（`photo` 的图片支持还没做，见 CLAUDE.md 的「还欠两处」） |

**颜色承担分类。** 六支色相在 OKLCH 的同一条明度带上取（亮色 L=0.485），
而不是随手挑六个纯色——随手挑的问题是明度乱跳，纯黄和纯蓝并排时黄的会「跳出来」、
蓝的会「陷下去」，六个一起看就是儿童积木。锁死明度只让色相转，六个色块的份量才一样重。

中性色全部带暖色相而不是纯灰：纯灰在任何有色底旁边都会被反衬出脏的蓝味。

**不用 `opacity` 表达弱化。** 它会乘到前景色上，而对比度是按变量值算的——
这一版有两处因此掉到 AA 以下（「还有 N 条」3.57:1、「生长中」3.96:1）而没被发现。
要弱化就改颜色变量或改形状（`seed` 用空心圆点、`growing` 用实心），别改透明度。

**暗色不是把亮色反过来。** 亮色下卡片靠阴影浮起；暗色下阴影是看不见的（黑底上叠黑），
层次改由亮度承担——越靠上的层越亮，再补一条发丝描边。六支颜色同理：暗色是同色相、
降饱和、提亮度重新取的一组，照搬亮色那六支到黑底上会刺眼到没法读。

选定这一版之前做过五个方案（两版克制的、三版卡片的）和四条配色路线，
过程与实测数据在 `audit/`。那个目录是**过程**，不是结论——真要改设计读这里。

## 7. 明确不做

- **纯私密日记不上网**。哪怕有登录。服务器会被打、cookie 会泄、配错一行就全公开。
  站上只放「至少愿意给一个人看」的东西——这个标准还有个副作用：心里有读者，写出来的东西才可读。
- **不自造静态站生成器**。那是用建站代替写站。
- **不做开放注册、不做评论系统**（真需要再挂第三方）。
- **不做算力接口。** 曾经计划过「跑编译器的可交互 demo」，并且当时就标注它是整个项目里
  唯一真正危险的部分——要容器隔离 + 超时 + 内存限制 + 无网络，还有 tsgo 那边一次真实的
  信息泄露作为前车之鉴（`rootDir` 只报诊断不拦读取）。现在明确划掉。
  理由不是做不了，是**它和这个站是两件事**：这里的单位是「条目」，一个能跑用户代码的
  沙箱不是条目，它是一个需要独立运维、独立安全模型、独立值班的服务。
  ARCHITECTURE 第 1 节说过动态站的代价——「有个要维护的服务在跑，你会潜意识不想碰它，
  站就死了」。真想做那个 demo，它该是自己的项目，挂自己的域名。
